<tool id="hictk" name="hictk" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@_1" profile="22.05">
    <description>Build juicebox hic</description>
    <macros>
        <token name="@TOOL_VERSION@">0.0.8</token>
        <token name="@VERSION_SUFFIX@">0</token>
    </macros>
    <requirements>
        <requirement version="@TOOL_VERSION@" type="package">hictk</requirement>
        <requirement version="0.3.7" type="package">pairix</requirement>
        <requirement version=" " type="package">samtools</requirement>
        <requirement version="0.3.7" type="package">pairix</requirement>
    </requirements>
    <version_command><![CDATA[echo "@TOOL_VERSION@"]]></version_command>
    <command><![CDATA[
#if $buildOpt.build == "yes":


#else
      hictk convert -f --output-fmt 'hic' '$incool' '$outhic'
#end if
    ]]></command>
    <inputs>
        <conditional name="buildOpt">
           <param help="Build and convert or just convert?" label="Optional build from mapped HiC reads" name="build" type="select">
                <option selected="True" value="no">Input a pre-built cool file and convert. Do not build from mapped reads</option>
                <option value="yes">Input a pair of mapped HiC read files, build and convert</option>
            </param>
            <when value="yes">
                <repeat max="2" min="2" name="samFiles" title="Sam/Bam files to process (forward/reverse)"
                help="Please use the special BAM datatype: qname_input_sorted.bam and use for 'bowtie2' the '--reorder' option to create a BAM file.">
                    <param name="samFile" type="data" format="sam,qname_input_sorted.bam"/>
                </repeat>
                <param name="dbfake" type="text" label="Provide the dbkey to be used for the cool file output" />
            </when>
           <when value="no">
                <param name="incool" type="data" format="cool,mcool,scool" label="cool/hdf5 data to convert to hic binary" />
            </when>
        </conditional>
    </inputs>
    <outputs>
        <data name="outhic" format="juicebox_hic" label="hictk convert on $incool.element_identifier hic" hidden="false"/>
    </outputs>
    <tests>
        <test>
            <param name="incool" value="incool_sample"/>
            <output name="outhic" value="outhic_sample" compare="sim_size" delta_frac="0.1" ftype="juicebox_hic" />
        </test>
    </tests>
    <help><![CDATA[
 This tool converts cool/mcool/scool format files into hic binary files

 **hictk**

 hictk is a blazing fast toolkit to work with .hic and .cool files.

 This repository hosts `hictk`: a set of CLI tools to work with Cooler, as well as `libhictk`: the C++ library underlying `hictk`.

 hictk_ is capable of reading files in `.cool`, `.mcool`, `.scool` and `.juicebox_hic` format (including hic v9) as well as writing `.cool` and `.mcool` files
 but this tool only writes `juicebox_hic` at present.

.. _hictk: https://github.com/paulsengroup/hictk

  ]]></help>
    <citations>
    <citation type="bibtex">@article{hictk,
author = {Roberto Rossini},
year = {2023},
url = {https://github.com/paulsengroup/hictk},
title = {hictk: blazing fast toolkit to work with .hic and .cool files}
}</citation>
    <citation type="doi">10.1093/bioinformatics/bts573</citation>
  </citations>
</tool>
